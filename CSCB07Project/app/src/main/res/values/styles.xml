<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/holo_primary</item>
        <item name="colorPrimaryDark">@color/holo_primary_dark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.AppCompat.NoActionBar.Light" parent="Theme.AppCompat.NoActionBar" />

    <style name="test" parent="android:Theme.Holo">
        <item name="android:colorBackground">@color/background_holo_dark</item>
        <item name="android:colorForeground">@color/bright_foreground_holo_dark</item>
    </style>

</resources>
